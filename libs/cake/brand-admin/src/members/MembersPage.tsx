import { SectionHeading } from "@danklabs/cake/pattern-library/core";

export async function MembersPage({ slug }: { slug: string }) {
  return (
    <div>
      <SectionHeading>Members</SectionHeading>
      <div>content</div>
    </div>
  );
}
